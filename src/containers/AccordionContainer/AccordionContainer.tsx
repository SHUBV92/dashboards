import Accordion from '../../components/Accordion/Accordion';

const AccordionContainer = () => {
  return (
    <div>
      <Accordion title={'London'} content={'It is a bigsss deal'} />
    </div>
  );
};

export default AccordionContainer;
