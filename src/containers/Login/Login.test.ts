import { render, screen, waitFor, cleanup } from "@testing-library/react";
import Login from './Login'


describe("Login", () => {
    xit("Login in with the correct user shows the Private routes", () => {
        // const { getByText } = render(<Login />)

    // expect().toBe()
    })
})